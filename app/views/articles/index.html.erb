<h1>Index</h1>

<% @articles.each do |article| %>
  <br>
  <%= article.title %>
  <br>
  <%= article.url %>
  <br>
  <%= article.user %>
  <br>
  <%= link_to("Show Article", article) %>
<% end %>
<%= link_to("New Article", new_article_path) %>